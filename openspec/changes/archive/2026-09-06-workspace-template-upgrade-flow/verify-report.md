# Verify Report: Workspace Template Upgrade Flow (2026-09-06)

## Gates run

| Gate | Command | Result |
| :--- | :--- | :--- |
| Template catalog drift gate | `node scripts/template-catalog.mjs --check` | ✅ exit 0 |
| Template catalog tests | `node scripts/template-catalog.test.mjs` | ✅ 3/3 |
| Preflight suite (incl. Tier 3) | `node actioNN/skills/nn-preflight/scripts/preflight-check.test.js` | ✅ 17/17 |
| Upgrade detection tests | `node actioNN/skills/nn-preflight/scripts/upgrade-check.test.js` | ✅ 6/6 |
| Backup script tests | `node actioNN/skills/nn-upgrade/scripts/backup-workspace.test.js` | ✅ 5/5 |
| Typecheck (iNNfo packages + editor) | `npm --prefix iNNfo run typecheck` | ✅ pass |

## What landed

- `iNNfo/specs/templates/catalog.json` — committed Level-2 template catalog (12 templates),
  generated by `scripts/template-catalog.mjs` (`--check` drift gate). `adopted` = highest
  published `template_version` per name.
- `actioNN/skills/nn-preflight/scripts/upgrade-check.js` — Tier-3 read-only scan:
  `gapKind`/`compareVersions` (numeric SemVer), `parsePinnedUrl` (flat + package layouts),
  `discoverModels`, `scanWorkspaceUpgrades` (current / upgrade-available / ahead / unlisted
  / unpinned), CLI.
- `preflight-check.js` — wires the scan under `--workspace-dir` (only when Level-3 models
  exist), `--template-catalog-url` override, `summary.template*` fields, informational
  `template-upgrade` items. **Exit code never flips** for an available upgrade; offline
  catalog degrades to a notice.
- `actioNN/skills/nn-upgrade/` — new skill (SKILL.md + `backup-workspace.js`): detect →
  consent `[a] Upgrade / [b] Continue` → out-of-workspace backup → schema-impact analysis
  (mapping questions only for removed/renamed/re-typed used definitions) → repoint +
  re-validate → confirm.
- Docs: `nn-upgrade` skill page (+ sidebar), `nn-preflight` page Tier-3 + version
  `V_0-1-2`, `nn-preflight/SKILL.md` Tier 3, `docs/use/manifest.md` preflight version label.
- `openspec/backlog.md` #8 — detailed deferred item for local-specialization migrations
  (out of scope here: detected `unlisted`, not migrated).

## Regression notes

- All 15 pre-existing `preflight-check.test.js` cases pass unchanged; the Tier-3 block only
  triggers when the workspace contains Level-3 models, so existing workspace fixtures do no
  catalog I/O.
- No `innfo-core` / `innfo-mcp` engine changes; typecheck green.

## Deferred (release-time)

- `nn-preflight` SKILL.md `version` bumped to `V_0-1-2`; the `docs/use/manifest.md` version
  label is synced but the **commit pin** stays at the old ref — the pin update rides the
  normal release flow (`nn-dev-release`), not this change.
- Local-specialization migration → backlog #8.

## Guard note

Changes are **uncommitted** (no commit/PR was requested). Work landed on the current branch
because the working tree carries uncommitted files from a concurrent session; switching
branches would clobber them. Only the files listed above were touched.