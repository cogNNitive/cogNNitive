---
name: nn-dev-development
description: Internal developer skill for cogNNitive maintainers. Guards the shared working tree against concurrent AI agents and enforces single-branch (dev) hygiene with batched merges to main. Trigger: any development session, concurrency check, branch decision, working-tree guard, session start.
---

# nn-dev-development (pointer)

This is a **pointer stub**, not the skill itself. Claude Code's native `Skill`
tool only resolves skills under `.claude/skills/<name>/SKILL.md`, but the
authoritative content for this maintainer-only skill lives at:

```
.agents/skills/nn-dev-development/SKILL.md
```

**Read that file now, in full, and follow it exactly as written.** Do not
treat this stub as the skill's instructions, and do not duplicate or
paraphrase its content here — this file must stay a thin pointer so the two
copies never drift.
